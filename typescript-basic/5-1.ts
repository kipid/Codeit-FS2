// Literal type
let productName = '바람막이';
const productName2 = '바람막이2';

let small = 95;
const large = 100;

function printSize(size: number) {
	console.log(size);
}

printSize(small);
printSize(large);
